# Portfolio assets

`resume.pdf` is the public resume used by the website. Replace it with an updated PDF using the same filename.

For future projects, create `images/` for photos and `reports/` for PDF reports, then reference those files from `data.js` with paths such as `assets/images/my-project.jpg` or `assets/reports/my-report.pdf`.
